#include<bits/stdc++.h>
using namespace std;
/*Questions Neha gives you a prime number P and a integer N close to P
and ask you to find N! modulo P
*/

/*
Fermat little theorem states that if p is a prime number, then for any integer a,
the number (a^p) - a is an integer multiple of p
moreover if a is not divisible by p  than (a^(p-1))-1 is an integer multiple
of p
*/  
// this theorem is generally used to find the modulo inverse
// to find the modulo in verse of a number 'a'
// use a^-1  = (a^(p-2))/(mod{p})
int main() 
{  

       
    
    cout<<endl;
    return 0;
  }